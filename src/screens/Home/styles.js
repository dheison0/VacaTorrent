import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  flex: { flex: 1 },
  container: {
    flexDirection: 'row',
    margin: 15,
  },
  input: {
    flex: 1,
    padding: 4,
    marginRight: 10,
    borderWidth: 2,
    borderColor: '#11A',
    borderRadius: 3
  }
});

export default styles;